const express = require('express');
const router = express.Router();

// Placeholder: you can add real endpoints later
router.get('/', (req, res) => {
  res.json({ message: 'Cart endpoint placeholder' });
});

module.exports = router;
